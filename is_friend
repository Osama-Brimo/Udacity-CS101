def is_friend(name):
    if name[0]=='D':
        return True
    return False
#print is_friend('Pickhead')

def is_friend1(name):
    if name.find('D')==0:
        return True
    else:
        return False
